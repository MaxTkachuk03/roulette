import 'package:flutter/material.dart';

const gradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [
    Color.fromRGBO(128, 119, 228, 1.0),
    Color.fromRGBO(241, 180, 136, 1.0),
  ],
);

const darkSlateGray = Color.fromRGBO(47, 79, 79, 0.8);

const darkCyan = Color.fromRGBO(0, 139, 139, 1.0);

const aqua = Color.fromRGBO(0, 255, 255, 1.0);

const dimGrey = Color.fromRGBO(105, 105, 105, 0.9);

const lightSlateGray = Color.fromRGBO(119, 136, 153, 0.9);

const darkSlateBlue = Color.fromRGBO(72, 61, 139, 0.7);

const mediumSpringGreen = Color.fromRGBO(0, 250, 154, 1.0);

const darkOliveGreen = Color.fromRGBO(85, 107, 47, 1.0);
